#include "main.h"

/**
 * _abs - finds the absolute value of an integer
 * @b: parameter
 * Return: b or -b
 */

int _abs(int b)
{

	if (b < 0)
		return (-b);
	else if (b >= 0)
	{
		return (b);
	}
return (b);
}
