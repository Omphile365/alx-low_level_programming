#include "main.h"

/**
 * _abs - finds the absolute value of an integer
 * @r: parameter should be checked
 * Return: r or -r;
 */

int _abs(int)
{
	if (r < 0)
		return (-r);
	else if (r >= 0)
	{
		return (r);
	}
}
